# Legacy Streamlit app

This folder contains the original Streamlit prototype.

## Run

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

API keys can be provided in the UI or via `./.streamlit/secrets.toml` (see [Streamlit secrets](https://docs.streamlit.io/develop/concepts/connections/secrets-management)).
